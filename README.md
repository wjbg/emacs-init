# Emacs configuration

This is my emacs configuration. Nothing special, actually, and mainly
for own use. In case you do want to use it, clone the repo to
`~/.emacs.d`. The packages will be installed automatically using
[`use-package`](https://github.com/jwiegley/use-package).
